require 'spec_helper'

describe BreatheIn do
  it 'has a version number' do
    expect(BreatheIn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
